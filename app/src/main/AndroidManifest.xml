<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.codetribe.quizmania">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".SportsActivity"
            android:label="Sports Activity" />
        <activity
            android:name=".MindActivity"
            android:label="Mind Activity" />
        <activity
            android:name=".PuzzleActivity"
            android:label="Puzzle Activity" />
        <activity
            android:name=".ResultsActivity"
            android:label="Results Activity" />
        <activity android:name=".SendActivity" android:label="Send Comment"/>
        <activity android:name=".RetrieveActivity" android:label="Retrieve Activity"></activity>
    </application>

</manifest>